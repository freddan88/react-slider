import React from 'react';
import PropTypes from 'prop-types';
import './Credits.css';

const Credits = props => {
    return (
        <div>
            
        </div>
    );
};

Credits.propTypes = {
    items: PropTypes.object
};

export default Credits;